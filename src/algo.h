#ifndef ___DST_ALGO_H
#define ___DST_ALGO_H

#include "algobase.h"
#include "tempbuf.h"
#include "dst_algo.h"
#include "dst_numeric.h"

#ifdef __DST_USE_NAMESPACES

// Names from "dst_algo.h"
using __DSA::adjacent_find;
using __DSA::binary_search;
using __DSA::count;
using __DSA::count_if;
using __DSA::equal_range;
using __DSA::find;
using __DSA::find_end;
using __DSA::find_first_of;
using __DSA::find_if;
using __DSA::for_each;
using __DSA::generate;
using __DSA::generate_n;
using __DSA::includes;
using __DSA::inplace_merge;
using __DSA::is_heap;
using __DSA::is_sorted;
using __DSA::lower_bound;
using __DSA::max_element;
using __DSA::merge;
using __DSA::min_element;
using __DSA::next_permutation;
using __DSA::nth_element;
using __DSA::partial_sort;
using __DSA::partial_sort_copy;
using __DSA::partition;
using __DSA::prev_permutation;
using __DSA::random_sample;
using __DSA::random_sample_n;
using __DSA::random_shuffle;
using __DSA::remove;
using __DSA::remove_copy;
using __DSA::remove_copy_if;
using __DSA::remove_if;
using __DSA::replace;
using __DSA::replace_copy;
using __DSA::replace_copy_if;
using __DSA::replace_if;
using __DSA::reverse;
using __DSA::reverse_copy;
using __DSA::rotate;
using __DSA::rotate_copy;
using __DSA::search;
using __DSA::search_n;
using __DSA::set_difference;
using __DSA::set_intersection;
using __DSA::set_symmetric_difference;
using __DSA::set_union;
using __DSA::sort;
using __DSA::stable_partition;
using __DSA::stable_sort;
using __DSA::swap_ranges;
using __DSA::transform;
using __DSA::unique;
using __DSA::unique_copy;
using __DSA::upper_bound;

// Names from dst_heap.h
using __DSA::make_heap;
using __DSA::pop_heap;
using __DSA::push_heap;
using __DSA::sort_heap;

// Names from "dst_numeric.h"
using __DSA::accumulate;
using __DSA::adjacent_difference;
using __DSA::inner_product;
using __DSA::iota;
using __DSA::partial_sum;
using __DSA::power;

#endif /* __DST_USE_NAMESPACES */

#endif /* ___DST_ALGO_H */