/******************************************************************************************

 ******************************************************************************************/

#pragma once

template <typename T> T PQ_LeftHeap<T>::getMax() //获取非空左式堆中优先级最高的词条
{ return _root->data; } //按照此处约定，堆顶即优先级最高的词条